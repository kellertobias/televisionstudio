import React, { useState } from 'react';

import { TStatusMessage } from '@/shared/api-types/status';

export const ConnectionToast: React.FC = () => {
	const [message, setMessage] = useState<TStatusMessage>();

	return null;
};
